name: goreleaser-test
on:
  push:
    branches:
      - main
    paths:
      - ci/.goreleaser.yml
  pull_request:
    paths:
      - ci/.goreleaser.yml
jobs:
  test-goreleaser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-go@v3.3
        with:
          go-version: 1.19

      - uses: goreleaser/goreleaser-action@v3.1
        with:
          distribution: goreleaser
          version: latest
          args: release --snapshot -f ci/.goreleaser.yml
